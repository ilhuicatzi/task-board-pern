export const getAllTasks = (req, res) => res.json({message: "Get all tasks"});

export const createTask = (req, res) => res.json({message: "Create a task"});

export const getTask = (req, res) => res.json({message: "Get a task"});

export const updateTask = (req, res) => res.json({message: "Update a task"});

export const deleteTask = (req, res) => res.json({message: "Delete a task"});

